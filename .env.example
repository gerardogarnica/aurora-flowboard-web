VITE_API_BASE_URL=

# development | staging | production
# Deployment environment. Drives the non-production environment ribbon.
# Leave empty for local development; CI passes it as a Docker build-arg.
# An absent or unknown value is treated as `development` (the ribbon shows).
VITE_APP_ENV=
